apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: nginx
    volumeMounts:
        - name: host-root
          mountPath: "/hostroot"
  volumes:
  - name: host-root
    hostPath:
      # directory location on host
      path: /